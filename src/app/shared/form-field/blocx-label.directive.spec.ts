import { BlocxLabelDirective } from './blocx-label.directive';

describe('BlocxLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new BlocxLabelDirective();
    expect(directive).toBeTruthy();
  });
});
