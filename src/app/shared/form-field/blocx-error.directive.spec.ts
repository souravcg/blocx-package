import { BlocxErrorDirective } from './blocx-error.directive';

describe('BlocxErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new BlocxErrorDirective();
    expect(directive).toBeTruthy();
  });
});
